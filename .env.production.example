# Production Environment Variables Template
# Copy this to Cloud Run environment variables

# Google OAuth2 Configuration
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here

# IMPORTANT: Update this with your actual Cloud Run URL after deployment
# Format: https://[service-name]-[hash]-[region].run.app/auth/callback
REDIRECT_URI=https://your-service.run.app/auth/callback

# Server Configuration
PORT=8080

# Required OAuth Scopes (for reference, not used in code):
# - https://www.googleapis.com/auth/calendar
# - https://www.googleapis.com/auth/gmail.send
